package dynamos.context
codec-package dynamos.context.serialization
codec-name context

namespace context "http://oh.rats.i/forget/this"

type contextItem {
     string name
     object value
     string? privacy
}

type location {
     double longitude
     double latitude
}

type activity {
     string name
}
